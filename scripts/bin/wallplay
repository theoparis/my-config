#!/bin/zsh
set -ex

[[ -z "$WALLFLAGS" || ${#WALLFLAGS[@]} -eq 0 ]] && WALLFLAGS=(-g 1920x1080+0+0)

xwinwrap ${WALLFLAGS[@]} -ov -ni -- \
	mpv \
		--vo=gpu-next \
		--hwdec=vaapi \
		-wid WID \
		"${1:-"https://www.youtube.com/playlist?list=PLRBp0Fe2GpgnIh0AiYKh7o7HnYAej-5ph"}"

