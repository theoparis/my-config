#!/bin/sh
discord \
    --ignore-gpu-blocklist \
    --enable-features=VapiVideoDecoder,WebRTCPipeWireCapture \
    --disable-features=OzonePlatform \
    --use-gl=desktop \
    --enable-gpu-rasterization \
    --enable-zero-copy
