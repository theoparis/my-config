command_timeout = 60
# Replace the "❯" symbol in the prompt with "➜"
[character]                            # The name of the module we are configuring is "character"g
success_symbol = "[➣](bold green)"     # The "success_symbol" segment is being set to "➜" with the color "bold green"

[cmd_duration]
min_time = 500
format = "⌛ [$duration](bold yellow)"

[git_status]
conflicted = "🏳"
ahead = "🏎💨"
behind = "😰"
diverged = "😵"
untracked = "🤷‍"
stashed = "📦"
modified = "📝"
staged = '[++\($count\)](green)'
renamed = "👅"
deleted = "🗑"

[username]
style_user = "white bold"
style_root = "black bold"
format = "👤 [$user]($style) "
disabled = false
show_always = true

[directory]
truncation_length = 8
truncation_symbol = "…/"
format = "📂 [$path]($style)[$read_only]($read_only_style) "


